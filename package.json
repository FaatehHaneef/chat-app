{
  "name": "quran-chat-app",
  "version": "1.0.0",
  "description": "AI-powered Qur'an Chat Application",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "setup": "node scripts/setup-db.mjs",
    "logs": "ls -1 .claude-logs/*.md | wc -l"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0"
  },
  "devDependencies": {},
  "workspaces": [
    "frontend"
  ]
}
