{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "frontend/dist",
  "regions": ["sfo1"],
  "functions": {
    "api/**/*.mjs": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  }
}
